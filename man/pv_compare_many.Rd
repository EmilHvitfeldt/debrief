% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{pv_compare_many}
\alias{pv_compare_many}
\title{Compare multiple profvis profiles}
\usage{
pv_compare_many(...)
}
\arguments{
\item{...}{Named profvis objects to compare, or a single named list of
profvis objects.}
}
\value{
A data frame with columns:
\itemize{
\item \code{name}: Profile name
\item \code{time_ms}: Total time in milliseconds
\item \code{samples}: Number of samples
\item \code{vs_fastest}: How much slower than the fastest (e.g., "1.5x")
\item \code{rank}: Rank from fastest (1) to slowest
}
}
\description{
Compares multiple profiling runs to identify the fastest. Useful for
comparing different optimization approaches.
}
\examples{
p1 <- pv_example()
p2 <- pv_example("gc")
p3 <- pv_example("recursive")
pv_compare_many(baseline = p1, gc_heavy = p2, recursive = p3)

# Or pass a named list
profiles <- list(baseline = p1, gc_heavy = p2)
pv_compare_many(profiles)
}
